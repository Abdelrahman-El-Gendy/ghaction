name: first workflow
on:
  push: 
      branches:
        - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: print a welcome msg
        run: echo "Welcome to out first Github Actions Workflows"
